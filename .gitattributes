# 260825 — este repo é BACKUP BYTE-EXATO da central, não fonte multiplataforma.
# Sem conversão de fim de linha em nenhum arquivo: os .cmd de 01_acoes PRECISAM
# de CRLF (com LF o cmd.exe come a primeira letra das linhas — 260825, o
# sincronizar-projetos.cmd imprimia 18 "OK" e copiava zero byte por causa
# disso). Deixar o git normalizar desfaria o conserto na primeira checagem.
* -text

# O CR continua byte-exato, mas não é espaço sobrando para o
# `git diff --check`: em Markdown histórico e batch ele é parte do fim de linha.
* whitespace=blank-at-eol,blank-at-eof,space-before-tab,cr-at-eol
