# megabrain

Protocolo de execucao multi-agente do Henrique (Iriq) - roda igual no
Claude Code, Kimi CLI e Gemini CLI. Ver SKILL.md pro protocolo completo.

**Estado deste push:** camada de sincronizacao de memoria de identidade
entre os tres agentes (.megabrain/memoria-global.md, mb-sync.py,
protocolo/references/260810_sync-memoria.md) + SKILL.md atualizado.

A biblioteca completa de referencias (260804_*.md), os scripts
operacionais (mb-new, mb-status, mb-doctor), os 4 arquivos de estado
de projeto e o restante do build local (tag v0.1.0) ainda nao estao
neste push - vem do repositorio local via git push separado.
