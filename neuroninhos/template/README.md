# Template Neuroninhos — vazio até o plano existir

Esta pasta vai guardar o template portável (config, scripts de detecção e
otimização, prompt de sistema) que instala o padrão Ollama/LM Studio + harness
megabrain em qualquer máquina/projeto.

Ainda vazia em 260909 — o desenho do que entra aqui é o entregável do agente
Opus 5 despachado nesta mesma sessão (ver `../HANDOFF.md`). Não adicionar
arquivo aqui antes de ler esse plano.
