2026-08-25 · v7.5 — auditoria de 3 agentes: o encanamento consertado, a versao com uma fonte so e o painel que responde por numero. MEMORIA: um licoes-megabrain.md orfao de 4,6 KB na raiz sombreava o canonico de 132 KB em memoria/nucleo e o indice rodou 1 dia com 8 licoes em vez de 166 — inclusive contaminando as copias de projeto; mb_utils.achar() passou a separar duas regras que estavam grudadas (nome CANONICO de PASTAS_RAIZ resolve pela pasta logica SEMPRE; caminho de MAQUINA mantem 'arquivo plano ganha'), fusao sem perda 166->174, orfa movida pra 99_to_delete com backup, regressao travada em test_orfao_na_raiz_nao_sombreia_canonico. mb-contexto.py para de anunciar '126+' chumbado e mede o total. RELATORIO: IGNORAR_CENTRAL casava por PEDACO de caminho e as entradas compostas '_github/export' e '_github/repo-local' nunca casavam — todo doc entrava 3x (948 KB -> 580 KB), com assert no import contra a proxima; eventos_hoje() derrubava a geracao inteira com AttributeError quando um evento trazia 'arquivo' como lista (o relatorio nao gerava em 25/08); o h1 lia PROGRESSO.json congelado na v6.7 e agora sai do VERSAO.txt, unica fonte de versao — MEGABRAIN.md dizia v3 e README.md v6.5, os dois perderam o numero e ganharam a regra. PAINEL: 01_acoes numerado de 1 a 11 com prefixo no NOME do arquivo, vindo de bin/mb_registro.py (registro declarado: botao novo entra no fim, numero aposentado nao volta); acoes, comandos de rotina e as 13 skills dele expansiveis no dashboard, com estado preservado entre os reloads de 15 s. ARTEFATOS 6->3, criterio um leitor por artefato: PAINEL-MEGABRAIN.html (2,7 MB, zero href apontando pra ele), CATALOGO-VISUAL.html e mb-painel.py pra 90_arquivo; ficam RELATORIO.html (ele), RELATORIO-DNA.html (terceiro replicando) e CATALOGO.md (agente). 4 scripts de uso unico arquivados, entre eles mb-patch-v5.py, que devolvia 8/8 FALHA e ainda escreveria VERSAO v5.0. GIT: central vira repositorio pela primeira vez, local e sem remote, com .gitignore proprio que VERSIONA dado pessoal (e o que precisa ser recuperavel) e .gitattributes '* -text' pra nao desfazer o conserto de CRLF. GATE 2 deixa de ser sensacao: o hook registra contexto_injetado (chars + pecas) e o gate passa a ter numero. Gate 5 vira botao (01_ABRIR-RELATORIO.cmd regenera e abre). CRLF: 13 .cmd estavam em LF-only e o cmd.exe comia a primeira letra das linhas — o sincronizar-projetos imprimia 18 OK e copiava zero byte desde 24/08; convertidos, com cheque_crlf no preflight e teste que reprova contra os originais. IDENTIDADE: o contrato de resposta apontava pra referencias/ (morto na v7.1) na FONTE e nos 5 globais — corrigido na fonte e propagado nos 6. PRIVACIDADE: sanitizacao por substring deixava o sobrenome colado no placeholder no pacote publico; token composto entra inteiro e um detector novo recusa placeholder colado em letra — a correcao ingenua (sobrenome solto) foi testada e reprovada, quebrava a URL publica do repo. /conclusao-megabrain vira ponteiro pro Gate 6 (carregava o teto de 2 perguntas revogado em 260824). Skills: as 5 do protocolo listadas pelo harness nos dois agentes, mais as 3 do Matt Pocock (MIT) instaladas; plugin 1.7.0. Suite 48 -> 70 -> 79. Decisoes 260825a-p.

Histórico completo: ver repositório privado da pasta central.
